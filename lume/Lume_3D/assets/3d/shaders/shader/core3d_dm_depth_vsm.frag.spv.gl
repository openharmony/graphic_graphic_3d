#version 450

layout(location = 0) out vec4 outColor;

void main()
{
    float _23 = dFdx(gl_FragCoord.z);
    float _26 = dFdy(gl_FragCoord.z);
    outColor = vec4(gl_FragCoord.z, fma(gl_FragCoord.z, gl_FragCoord.z, 0.25 * fma(_23, _23, _26 * _26)), 0.0, 0.0);
}

